import { ShortenstringPipe } from './shortenstring.pipe';

describe('ShortenstringPipe', () => {
  it('create an instance', () => {
    const pipe = new ShortenstringPipe();
    expect(pipe).toBeTruthy();
  });
});
